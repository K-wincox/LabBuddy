services:
  labbuddy-api:
    build: .
    container_name: labbuddy-api
    restart: unless-stopped
    env_file: .env
    ports:
      - "0.0.0.0:8088:8088"
    networks:
      - labbuddy-shared

networks:
  labbuddy-shared:
    external: true
