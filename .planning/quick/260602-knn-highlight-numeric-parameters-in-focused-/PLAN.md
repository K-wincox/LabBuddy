# Plan

## Scope

- Highlight numeric experiment parameters in focused Today step detail text.
- Apply the shared highlighter to the focused timeline expanded card and the run detail sheet.
- Keep data, persistence, and broader UI redesign out of scope.

## Verification

- `make preflight`
- `git diff --check`
- XcodeBuildMCP simulator build/run
